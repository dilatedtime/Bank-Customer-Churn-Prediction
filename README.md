# Bank-Customer-Churn-Prediction
A concise ML framework for customer churn prediction, covering data preprocessing (SMOTE, encoding, outlier removal, scaling), model training (Decision Tree, Random Forest, KNN), hyperparameter tuning, and evaluation.
